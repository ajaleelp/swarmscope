# Copy this file to .env before starting local services.
# The real .env is ignored by Git and must only contain local development values.
HINDSIGHT_POSTGRES_USER=hindsight
HINDSIGHT_POSTGRES_PASSWORD=replace-with-local-password
HINDSIGHT_POSTGRES_DB=hindsight
HINDSIGHT_POSTGRES_PORT=5432
